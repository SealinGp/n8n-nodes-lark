import { ResourceType } from '../../help/type/enums';
import { ResourceOptions } from '../../help/type/IResource';
import { WORDING } from '../../help/wording';

export default {
	name: WORDING.ResourceWiki,
	value: ResourceType.Wiki,
	order: 145,
} as ResourceOptions;
